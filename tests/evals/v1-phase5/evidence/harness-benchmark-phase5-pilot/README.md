# Phase 5 Pilot Benchmark Evidence

Pilot: `harness-benchmark-phase5-pilot`

Starting revision: `090f6d1c33d9f006cc8e95491badc33a8053c89f`

Model lock: `gpt-5.6-sol`, reasoning `high`

External trust was asserted by the orchestrator at `2026-07-18T06:40:32Z`.
This local evidence directory records the real baseline run. It is not an
authenticated publication: the operator did not receive an external trust
registry, public key, private key, or signature authority and did not generate
keys or sign anything.

Digest convention for `environment_sha256` and `result_sha256`: SHA-256 of
compact, UTF-8 JSON with sorted keys after omitting the digest field, matching
the Phase 5 verifier's canonical digest function.

All repository mutations, seeds, repairs, transcripts, and artifacts are
confined to this worktree. No remote mutation, push, deployment, key generation,
or signing is authorized.
